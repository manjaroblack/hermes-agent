# Jev closed-pool reasoning effort — binding design v1
role: orchestrator; source: PRD.md + ANALYSIS.md; board: hermes-jev; project: p_7211c32b
status: design candidate; implementation_authorized=false; approved_command_set=[]
endpoint: two independently GO-reviewed feature PRs ready for STAG merge/install decision; NOT merged, installed, live-verified, or deployed.

## Pins / boundaries
fork: manjaroblack/hermes-agent; base=local/runtime; SHA=c5e435a8998090dd2cf4823283bff4aa8c2f47a0.
plugin: manjaroblack/hermes-typesafe; base=main; SHA=b3fe987015793feb9496ee2e16631646afa7348a.
code staging: persistent unmanaged dir workspaces /tmp/hermes-jev-effort-t_66770f64-host and /tmp/hermes-jev-effort-t_66770f64-plugin; never managed scratch. Each owns isolated clone at <workspace>/repo plus feature worktree <workspace>/worktree; retain through review. Worker creates after GO; orchestrator never creates product checkout. Initial feature branch fix/jev-closed-pool-effort within each separate repo. No shared-checkout branch switch/reset; no worktree under live install. If paths nonempty/unrelated, STOP, preserve.
base admission: fetch exact pins; verify object + remote identity and branch tip. Drift since design -> STOP/reconcile; never silently select moving tip. Plugin source base remains its own main pin; only integration fixture points to reviewed new fork head.
forbidden: edit /root/.hermes/hermes-agent, /root/.hermes/plugins/typesafe, ~/.hermes/config.yaml, credentials/profiles; alter live routing; merge/install/cutover/restart; Nous PR; paid live inference; catalog/dependency feature expansion; suggestion UX change; invented model suffixes/SKUs.

## D1 — closed pool / normalization
required identity: model + provider strings; retain existing identifier bounds, closed label roster, duplicate-identity checks.
extra entry keys: allowed but discarded unless recognized below; never alone invalidate model routing.
recognized optional fields: reasoning_allowed, reasoning_default.
effort enabled iff allowed is nonempty actual list of distinct exact lowercase strings in current authorized product ceiling low|medium|high AND default is exact member. This ceiling is product policy, NOT replacement Hermes vocabulary. Empty/missing list, wrong types, unknown/duplicate token, absent/nonmember default -> omit BOTH effort settings for that label, preserve valid identity and other labels. Do not trim/coerce malformed effort tokens.
label policy for this delivery:
- cheap -> z-ai/glm-5.3-flash / openrouter; reasoning_allowed=[]; no default; no effort directive.
- coding -> gpt-5.6-luna / openai-codex; [low,medium,high]; default medium.
- reasoning -> grok-4.6 / xai-oauth; [low,medium,high]; default high.
no hardcoded live config write; examples/tests only. Empty legacy pool stays inert. Existing label roster retained; no new long-context SKU.
normalization owner: shared pure plugin pool normalization used by __init__._bounded_routing_models AND route._validated_pool; preserve detachment, no aliasing external config lists. Implementation may place narrow helper in new topical plugin module; not independent competing parsers.

## D2 — request / tolerant effort answer only
routing RPC count remains one. Existing target Choice + mismatch Noul + worth Noul + difficulty Score remain; append question key reasoning_effort, type choice, criteria = deterministic union of valid nonempty allowlists. All-empty union -> OMIT question (no empty Choice, sentinel, or extra RPC). All questions/instructions/threshold policy stay questions.py.
SDK state stays exactly existing current user_message + current_label; no history/system prompt/current agent object; no new upload scalar required. Effort question describes selecting depth for target label. Mismatch/worth wording may address model OR effort suitability; keys/thresholds unchanged.
remote Choice -> intersect selected label: valid exact token in label list -> token; missing/malformed/wrong type/outside union/outside selected list -> selected default iff valid config; disabled label -> omission. Failure of effort answer alone must not cancel valid core route.
real-path requirement: client._normalize_response currently rejects missing/wrong Choice before evaluate_routing. Add local-only optional_choice_questions keyword (immutable tuple, default empty) through TypeSafeRuntime.execute_sync -> _submit closure -> AsyncTypeSafeClient.system_one -> _normalize_response. Only routing callers opt in with ("reasoning_effort",) when question exists. Validate bounded unique tuple, exact requested question names, each type choice before broker admission. No new broker ABI, no setting exposed to remote/SDK/tool schema, no inferred leniency from answer name alone. Keep default empty on execute_async/system_one tool/guards/suggestions. Preserve fake client compatibility by not passing new keyword when empty.
normalization: whole-envelope exact JSON/size/depth/model/usage bounds remain strict; core routing answers + unrelated extras retain current fail-closed validation. Malformed optional Choice only -> omit that answer from normalized output; evaluator performs per-label default. Never return raw unvalidated response after ClientError; never relax guards or arbitrary system_one requests; no retry/fourth RPC. Non-JSON/oversize envelope still fails entire call (bounded transport safety wins).

## D3 — eligibility / identity / permission
modes off or enabled!=true -> no routing calls/directives.
first turn: both first_turn and cache_break_if_worth_it may propose; first-turn gates = existing valid core response, unambiguous current pool identity, mismatch>=ROUTING_HIGH, target confidence>=ROUTING_HIGH; worth threshold not required. Emitted allow_cache_break=false (actual first-turn bool, not mode string).
later: first_turn does nothing. cache_break_if_worth_it requires same core gates PLUS worth>=ROUTING_HIGH; emitted allow_cache_break=true. Nonboolean first-turn payload -> no route. Keep numeric/finiteness/confidence/difficulty validation; difficulty never bypasses gates.
proposal-worthy: gates pass AND (identity changes OR valid intersected effort exists). Plugin need not see current effort; same-identity effort candidate goes to host, which compares actual effective effort and no-ops when equal. No bypass of mismatch/worth just because effort present.
apply in BOTH combined registered harness and standalone directive handler. Legacy advisory helper remains advisory, never writes runtime.

## D4 — generic host directive / ownership
wire schema remains model_switch{model,provider,allow_cache_break,reasoning_effort?}; model/provider/bool required unchanged. reasoning_effort optional exact string; absent/invalid = no plugin effort override, never reason to discard otherwise valid model switch.
trust split: plugin owns selected label membership/default; host knows only generic destination + effort and validates canonical/family support. No core import of TypeSafe, no host read of plugins.entries.typesafe, no new model label/allowlist field on wire. 'invalid plugin-proposed effort ignored' means final plugin intersection followed by host token validation; malicious local plugin code is not sandboxed by this feature.
canonical validation: reuse agent.reasoning_effort.EFFORT_LADDER and existing destination-family supported sets/predicates/codex_supported_efforts; use existing clamp at transport boundary. Unknown token/family or explicitly empty supported set -> skip effort. Recognized but destination-unsupported token -> skip, not force a nearby token (PRD rule10). Clamp is NOT authority to widen configured pool or convert an invalid proposal. No network capability probe on hook hot path.
composition: existing differing model/provider directives remain fail-closed. Same destination duplicates preserve first valid directive's effort AND cache permission; later duplicates cannot fill missing effort, override it, or widen permission. Generation stale or partial agent -> no mutation.
cache permission before any effect: first turn requires false; later requires true. Same model/provider with absent/invalid/equal effective effort -> false/no-op. Same identity + changed valid effort -> true when gates pass. Model switch with invalid effort -> existing model switch result.
host runtime seam: add optional keyword reasoning_effort=None to existing AIAgent.switch_model forwarding/runtime helper; parsing/family helper in narrow new topical sibling. Apply via reasoning_config using Hermes parser INSIDE existing switch snapshot/rollback transaction, after destination config resolution, before _primary_runtime capture. Never set a made-up agent.reasoning_effort Python attribute. Preserve unrelated request overrides and runtime capabilities. Do not add blind transport-specific nested request_overrides that bypass shared clamping; verify existing override conflicts cannot silently defeat selected effort or leak it onto another destination.
same-identity effort path uses current resolved runtime without unnecessary credential/network lookup; maintain rollback/snapshot guarantees and frozen prompt. Exact helper factoring is worker-owned, API/semantics above fixed. No source-wide runtime refactor; obey topical sibling guidance.
persistence: selected effort applies subsequent requests/turns for that runtime identity, captured in primary snapshot. Identity switch with effort omitted preserves existing model-switch config behavior; omission itself never forces low/none or clears user config. No stale Jev-specific effort wire override carried to cheap Flash. Flash acceptance = plugin emits NO effort; do not conflate that with overriding separately user-configured transport behavior.
frozen prefix: preserve _cached_system_prompt bytes, history, tool schemas, toolset/memory roster. Cache break means provider transport reuse eligibility, not rebuilding prompt.

## User stories / TDD evidence
US1 operator config compatibility: add effort keys without killing pool. Prove RED/GREEN both validators; absent keys old behavior; extra unrelated key ignored; malformed per-label optional settings don't poison valid siblings; no aliasing; defaults exact; Flash no effort.
US2 Jev bounded decision: one routing RPC, optional Choice union, intersection/default/omit. Prove full client/runtime normalization with synthetic SDK (not mocked evaluate_routing): missing/wrong-type/out-of-union/out-of-label effort survives with correct default; malformed core/extra arbitrary answer/envelope still fails; ordinary system_one and guard missing answers still fail. No RPC or timeout budget increase; combined routing+rank+rerank <= existing MAX_PRE_LLM_RPC.
US3 first/later effort-only: registered combined harness -> generic host -> next actual captured SDK request; same Grok model high->medium and same Luna medium->high. First false works; later false rejected; later true with all gates works; low mismatch/worth/confidence denies; already-selected effective effort no-op. Both routing entrypoints tested.
US4 host safety: unknown/unsupported/type-invalid effort omitted while valid model switch retained; destination clamp supported set reused; duplicate-first semantics; stale generation; rollback failure restores reasoning/runtime/prompt; frozen prompt/history/tools unchanged. Cross-provider Grok->Luna->Flash without stale effort leakage. Tests assert next request fields, not method call only.
US5 delivery: fork feature PR base local/runtime + plugin PR base main; each independent Grok GO exact head. Plugin installed-wheel/two-home composition pins new approved fork head in test constant AND CI fixture fetch, using actual plugin loader and actual host turn/request path with only external SDK/network replaced by deterministic synthetic endpoints.

## Verification / artifacts
host: read AGENTS routing files; focused tests/agent/test_plugin_pre_llm_switch.py + related switch/reasoning/transport contracts via scripts/run_tests.sh ONLY; broader affected contract regressions; changed-file lint/type/compat/diff checks. New production helper behavior tested; no source-text assertion tests.
plugin: uv sync --locked --extra test; existing .github/workflows/ci.yml commands; offline suite + installed wheel, reviewed-fork composition, legacy fixture/two-home + wheel/sdist build. Workflow's plugin pytest command is repository-native; host tests always scripts/run_tests.sh. Keep dependency lock unchanged unless indispensable approved test tooling; no new paid/network inference.
isolation: test subprocess before imports unsets inherited HERMES_KANBAN_DB/BOARD/HOME/TASK/WORKSPACE/WORKSPACES_ROOT, resets HOME/HERMES_HOME/kanban homes to private temp dirs; asserts resolved DB/workspace inside temp. This is TEST subprocess hygiene, never bypass worker mutation guards. No live credential/config reads by tests.
receipts: RED on pinned base, GREEN candidate commands+exit results, changed files, source/base/head SHAs, PR URL/base, exact-head CI URLs/status, no live changes, rollback. Worker self-check != independent review.
CI policy: host completion_contract=local-only because local/runtime has zero required contexts/rulesets; this does NOT waive feature PR, applicable CI, independent review, or permit zero evidence. Old PR-specific cancelled-job waiver NOT inherited. CI blockage -> honest needs_input + active-wake door, no workflow disable/waiver invented. Plugin completion_contract=manjaroblack/hermes-typesafe; both required Python 3.10/3.12 jobs exact-head green. Never merge from green checks.

## Candidate bounded commands (not authorization)
ACMD01: isolated clone/fetch only named manjaroblack repos, verify exact base pins, create own feature worktree; GitHub/read-only source inspection; no live source writes or upstream pushes.
ACMD02: host edits limited generic directive/runtime effort seam + necessary topical helper + behavioral tests/docs; plugin edits pool/routing/questions/harness/client/runtime optional-answer seam + related tests/docs/CI fixture pin; no unrelated guard/suggestion behavior or broker ABI rewrite.
ACMD03: install locked dev deps only in own staging venv, run isolated offline tests/lint/type/build/diff/secret scan; fixtures synthetic; read-only exact-head CI checks.
ACMD04: conventional commits, push own feature branches to manjaroblack fork/plugin, open feature PRs against local/runtime/main respectively; publish no secrets, no Nous PR, no merge/install/restart, no forced shared history rewrite.
rollback before merge: leave live state untouched; revise feature branch only. After future owner-authorized merge: omission of per-label settings or reviewed revert are proposals, not live authorization now.

## Graph / independent gates
root design -> hermes-security-audit -> hermes-review fresh design GO -> hermes-coding fork with SAME-card Grok -> hermes-coding plugin with SAME-card Grok. Root direct parent all; design GO direct parent both code cards; plugin also depends reviewed fork. No downstream QA/release/review child of coding cards; avoids premature parent completion protocol conflict.
code start iff fresh security GO + independent design GO bound to exact DESIGN.md SHA256, implementation_authorized=true, nonempty approved_command_set subset ACMD01..04, pins still match. Merge/live gates always closed despite GO.
security outputs SECURITY_GO or SECURITY_NO_GO + hashed report; design reviewer independently hashes design + audit, rejects conditional/stale/negative evidence.
NO_GO/redesign: hold SAME implementers; active-wake default front door automatically resume/create ONE design-only revision for hermes-orchestrator -> fresh security -> fresh Grok, relink SAME held code cards before release. No 'do it' re-quiz; no archive/kill without STAG. A blocked NO_GO is evidence, never a deadlocking parent for revision. No second implementer.
coding model: Luna/openai-codex verified default; one quality failure -> same card/worktree/PR to hermes-coding-sol with gpt-6-astra/openai-codex override via authorized control plane. Current profile label alone means Sol, not Astra. Clear writer override before same-card Grok review, verify actual model/provider. Never Grok implementation, never self-review/implementer merge.
terminal actions: root/security/design complete their own finished phase only; code request_review(reviewer=hermes-review) after verified PR delivery, reviewer complete only on GO or request_changes with actionable defects. needs_input reserved genuine decision/access/CI wall; notify+wake originating thread required.
